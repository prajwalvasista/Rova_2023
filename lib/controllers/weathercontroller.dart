// class WeatherController {
//   WeatherModel _weatherModel;
//   WeatherView _weatherView;

//   WeatherController(this._weatherModel, this._weatherView);

//   // Define methods to handle user input, update the model, and refresh the view
// }
